experiments:
  baseline:
    script: train.py
    epochs: 50
    learning_rate: 0.001
    requires_output_var: false

datasets:
  dataset_simple:
    root: /data/simple
    experiments:
      - name: baseline